# Git-rend
CLI application that talks to GitHub API and shows the trending repositories

A collection of lightweight Python scripts that connect the local computer terminal (CLI) to real-time web databases (APIs). These projects focus on validating user commands from the terminal, making safe internet requests, and neatly parsing raw data from the web.